import { VNode } from '../../core/VNodes';
/**
 * There is currently no support for switching same input between controlled and nonControlled
 * If that ever becomes a real issue, then re design controlled elements
 * Currently user must choose either controlled or non-controlled and stick with that
 */
export declare function processElement(flags: number, vNode: VNode, dom: Element, nextPropsOrEmpty: any, mounting: boolean, isControlled: boolean): void;
export declare function isControlledFormElement(nextPropsOrEmpty: any): boolean;
